package brightspark.runicmagic.block

import brightspark.runicmagic.model.RuneType
import net.minecraft.block.Block
import net.minecraft.block.material.Material

class AltarBlock(val runeType: RuneType) : Block(Properties.create(Material.ROCK)) {
	// TODO: Altar block
}
